{
    var y:int32 = 1;

    {
        var y:int32 = 10;
        y-1;
    }

    y+1;
    y+4; //Returns y+4 ("y = 1", so returns 5 (because 4+1 = 5) ) // Actually it doesn't return that (just parse_eval function prints that)
}

{
    var a:int32 = 4;
    var b:int32 = 5;

    var is_equal:int32 = (a == b);
    var result:int32 = (is_equal * 100) + (!is_equal * 200);
    result;
}


{
    var a:int32 = 4;
    var b:int32 = 5;
    var res:int32 = a * b;
    res;
}

{
    {  
        var a:int32 = 4;
        var b:int32 = a + 4;
        a = 3;
        var c:int32 = a + 4;
        var x:int32 = (b == c);
        x;
    }   
}
// I'll remove parser_eval function. The language will translate to assembly x86_64
